---
# e.g. CentOS 8 x86_64 or CentOS Stream 8 x86_64
nectar_default_image_name: "{{ ansible_facts['distribution'] }}{% if 'Stream' in ansible_facts['distribution_release'] %} {{ ansible_facts['distribution_release'] }}{% endif %} {{ ansible_facts['distribution_major_version'] }} {{ ansible_facts['architecture'] }}"
