---
# e.g. CentOS 8 x86_64 or CentOS Stream 8 x86_64
nectar_image_name: "{{ ansible_distribution }}{% if 'Stream' in ansible_distribution_release %} {{ ansible_distribution_release }}{% endif %} {{ ansible_distribution_major_version }} {{ ansible_architecture }}"
