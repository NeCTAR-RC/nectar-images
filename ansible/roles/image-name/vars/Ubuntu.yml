---
# Ubuntu 18.04 LTS (Bionic) amd64
nectar_default_image_name: "{{ ansible_facts['distribution'] }} {{ ansible_facts['distribution_version'] }}{% if 'LTS' in ansible_facts['lsb']['description'] %} LTS{% endif %} ({{ ansible_facts['distribution_release']|capitalize }}) {% if ansible_facts['architecture'] == 'x86_64' %}amd64{% else %}{{ ansible_facts['architecture'] }}{% endif %}"
