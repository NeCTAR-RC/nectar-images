---
- hosts: all
  become: yes
  gather_facts: yes

  vars_files:
    - vars/main.yml
    - vars/aiready.yml

  roles:
    # Initial Setup
    - role: image-name
    # infastructure layers
    - role: aiready-llm
    # Finishing up
    - role: banner
    - role: facts
    - role: cleanup
